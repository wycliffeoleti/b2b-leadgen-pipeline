name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          python-version: "3.12"
          enable-cache: true

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Lint
        run: uv run ruff check src/ tests/ workflows/ scripts/

      - name: Format check
        run: uv run ruff format --check src/ tests/ workflows/ scripts/

      - name: Type check
        run: uv run mypy src/ --strict

      - name: Test
        run: uv run pytest tests/ -v --cov=src --cov-report=term-missing
